/**
 * Types re-exported from @fin-catch/shared
 *
 * This file provides backward compatibility for existing imports using @/types.
 * All types are now defined in the shared package and re-exported here.
 */
export * from "@fin-catch/shared";

/**
 * Note: The shared package exports:
 * - Types: api, form, portfolio, benchmark, holdings, auth, sync
 * - Utils: formatters, constants
 * - Service interfaces: IPortfolioService, IPortfolioEntryService, etc.
 */
